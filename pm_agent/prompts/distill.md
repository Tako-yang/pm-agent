你是 MEMORY.md 的蒸馏专家。当前的 MEMORY.md 已超过 3000 字上限，请重写一份更精炼的版本。

# 要求

1. **保留所有 5 个固定段名结构**（顺序不变，每段以 `# 段名` 开头）：
   - 项目宪法（永远不变）
   - 当前架构（随实现演进）
   - 已知坑（worker 必读，避免重复踩）
   - 当前未完成任务的上下文（只放正在做的）
   - 已完成里程碑（一句话）

2. **段间压缩策略**：
   - "已完成里程碑"段允许大量合并归纳（多任务合并成一句）
   - "已知坑"段去重，相似坑合并
   - "当前未完成任务的上下文"段只保留 status=running 的任务，已完成的清出
   - "项目宪法"段不可压缩，原样保留（这是底线约束）
   - "当前架构"段中的设计决策子条目（"设计决策："开头的）不可压缩，原样保留

3. **总字数严格 ≤ 2500 字**（留 500 字给后续追加空间）

4. 输出仍然是合法的 markdown，每段以 `# 段名` 开头，内容紧跟其后

# 输入（旧 MEMORY.md）

{{old_memory}}

# 输出（新 MEMORY.md）

只输出新的 MEMORY.md 内容本身，不要任何额外的解释、代码栅或 meta 段。
